{
  "name": "res-scrapy",
  "version": "1.1.0",
  "description": "A web scraper using rescript",
  "type": "module",
  "main": "dist/main.mjs",
  "exports": {
    ".": {
      "import": "./dist/main.mjs"
    }
  },
  "scripts": {
    "test": "npm run res:test",
    "build": "npm run res:build",
    "date:build": "tsc -p tsconfig.date.json",
    "date:check": "pnpm run date:build && git diff --exit-code -- src/schema/v2/utils/date/*.mjs",
    "res:test": "retest ./test/res/*.res.mjs",
    "start": "node dist/main.mjs",
    "res:build": "rescript",
    "res:dev": "rescript watch",
    "prebundle": "pnpm run date:build",
    "res:clean": "rescript clean",
    "docs": "docsify serve docs",
    "release:check": "pnpm run res:build && pnpm run res:test && pnpm run bundle && pnpm run date:check",
    "bundle": "rolldown -c",
    "security:audit": "pnpm audit --audit-level=high",
    "res:lint": "rescript format --check",
    "prepare": "pnpm run bundle && node ./scripts/ensure-cli.mjs",
    "prepublishOnly": "pnpm run release:check",
    "res:coverage": "c8 pnpm run res:test"
  },
  "bin": {
    "res-scrapy": "dist/main.mjs"
  },
  "author": {
    "name": "José Martínez Santana"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/MetalbolicX/res-scrapy.git"
  },
  "bugs": {
    "url": "https://github.com/MetalbolicX/res-scrapy/issues"
  },
  "homepage": "https://github.com/MetalbolicX/res-scrapy#readme",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "rescript",
    "html",
    "scraper",
    "web scraping",
    "cli",
    "json",
    "data wrangling",
    "node.js"
  ],
  "license": "MIT",
  "dependencies": {
    "@rescript/runtime": "^12.3.0",
    "node-html-parser": "^9.0.1"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "c8": "^12.0.0",
    "rescript": "^12.3.0",
    "rescript-test": "^8.0.1",
    "rolldown": "1.2.2",
    "rollup-plugin-esbuild": "^6.2.1",
    "typescript": "^7.0.2"
  }
}
